name: First Workflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Hello Github Actions
      run: echo "First Github Actions Job"

    - name: List files
      run: ls

    - name: Read file
      run: cat README.mdF
